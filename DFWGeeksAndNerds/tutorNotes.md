Tutor Notes 03.15.26 


Regarding AI 

See how it did it, than do it on your own. 

Calander stuff would need to be connected to back end. 
Plug in. 

Setting up the database MVC 

Front ends don't really connect to front ends. 

how it works now 
create an api 
api connects to the database 
https encryption manages security for the api 

api gets tokens 

mock data 

make images as similar sized as possible. 

16:9 ratio didn't work so do the 1280 x something. 


565 X 317

the api is outside of the mvc project 
api can work similar to doordash 
api allows the user to connnect to the resturant network. 

mvc app is the client
api is doordash 
database is the resturant 

database gives the api the events it needs. 

api's can connect to more than just databases. 

rest api's can get users, create, save, update, users 


Google These Terms later 
graphql api 
functional api's 

I need to render from the razor page a calander that I can use with an api. 

Explain how to build a simple monthly calendar in an ASP.NET MVC Razor view.

Requirements:
- Use standard MVC Razor (not Blazor).
- Use basic HTML elements (table/div/tr/td) for the layout.
- Use Razor syntax (@for, @foreach, conditionals) to iterate and render the calendar days.
- No JavaScript calendar libraries or third-party UI components.
- Prefer server-side rendering.

Provide:
1. A ViewModel that represents the calendar month and days.
2. Controller code that generates the data for the selected month.
3. A Razor view that renders the calendar grid.
4. Logic for handling blank cells before the first day and after the last day of the month.
5. A small CSS example so the calendar layout looks organized.
6. A short explanation of how the Razor loops build the calendar.

Takeaways: 

When you create an api, you need to set your website as the setupr project otherwise it will target the api which won't start right away.

Zoom Meeting Notes From 03.15.26 - (Sessions End Date) 

endpoints are like functions. it will return information. 

