@using DFWGeeksAndNerds.Models;
@using Newtonsoft.Json
@model CalanderViewModel;

@{
    ViewData["Title"] = "Events Page";
}

@section Styles {
    <link rel="stylesheet" href="~/css/EventCalendar.css" asp-append-version="true" />
}

<div>
    <div class="container"> 
        @* @{
            <!-- TODO CREATE MONTHS-->
            string monthName = DateTime.Now.ToString("MMMM");
            <!-- To get all of the months in a list --> 
            List<string> monthNames = new List<string>();
            <!-- Loop through the months and add them to the list -->
            for (int i = 1; i <= 12; i++)
            {
                monthNames.Add(new DateTime(DateTime.Now.Year, i, 1).ToString("MMMM"));
            }
        } *@

        @{
            DateTime dtCurrentMonth = new DateTime(Model.Year, Model.Month, 1);


            // Get the number of days in the current month
            <!--I need the current month to be displayed on the page -->
            string currentMonth = dtCurrentMonth.ToString("MMMM");
            string currentYear = dtCurrentMonth.ToString("yyyy");
            int daysInMonth = DateTime.DaysInMonth(dtCurrentMonth.Year, dtCurrentMonth.Month);
            int weeksInMonth = (int)Math.Ceiling(daysInMonth / 7.0);
            int currentDay = 1;
            
            
        }

        <!--Display the month name -->
        <div class="row">
            <h1 class="col-4">@currentMonth @currentYear</h1>
            <form model="@Model" method="post">
            <input type="hidden" name="modelJson" value="@JsonConvert.SerializeObject(Model)"/> 
                <button type="submit" formaction="@Url.Action("PrevMonth", "Events")" class="col-2 btn btn-secondary mx-3">prev</button>
                <button type="submit" formaction="@Url.Action("NextMonth", "Events")" class="col-2 btn btn-primary mx-3">next</button>
            </form>
        </div>
        
        <!-- I need a row to show the days of the week --> 


    
    <div class="row">
        @for (int count = 0; count <= (int)DayOfWeek.Saturday; count++)
        {
            // int i = (startIndex + count) % daysOfWeek.Count;
            <div class=" daysOfWeekRow calander-header col-1 text-center fw-bold">
                @Enum.GetNames(typeof(DayOfWeek))[count]
            </div>
        }
    </div>
        @{
            // 01:25:59 
            <!-- We need a start date for the calander -->
            // this is the number of blocks that need to be disabled before the first day of the month.  -->
            int startBlock = 7 - (int)dtCurrentMonth.DayOfWeek;
            // this is the number of blocks that need to be disabled after the last day of the month.  -->
            int endBlock = DateTime.DaysInMonth(dtCurrentMonth.Year, dtCurrentMonth.Month) - 1;
            int blockCount = 0;

        }
        <!-- 5 rows needed for the 4 weeks and extra days -->
        @for(int y = 0; y <= weeksInMonth; y++)
        {
            <div class="row">
                <!-- 7 columns for the 7 days of the week -->
               

                @for(int x = 1; x < 8; x++)
                {
                    
                    @if (blockCount <= startBlock)
                    {
                        // this is the number of blocks that need to be disabled before the first day of the month.  -->
                        <div class="col-1 border border-dark calander-box calander-box-disabled"></div>                   
                        
                    }
                    else if (blockCount > endBlock + startBlock)
                    {
                        // If the block count is greater than the number of days in the month we don't want to make those days accessible.
                        <div class="col-1 border border-dark calander-box calander-box-disabled"></div>
                    } 
                    else
                    {
                        // this is a filter where we are looking at the events and seeing if there are any events that match the current day and month. If there are we want to display the event name in the calander box.
                        var eventsForDay = Model.CurrentEvents(currentDay);
                       // if the events for the day is not empty in its explanation and there is at least on event....
                        if (eventsForDay != null && eventsForDay.Count > 0)
                        {
                            // show that event in the calander box.
                            <div class="col-1 border border-dark calander-box">
                               <div>
                                   @currentDay 
                               </div>

                               @foreach (var eventItem in eventsForDay)
                               {
                                   <div class="calander-event-indicator">@eventItem.DisplayName</div>
                               }
                            </div> <!-- Close the calendar box here -->
                        }
                        // If there are no events...
                        else
                        {
                            <div class="col-1 border border-dark calander-box">
                                <div>@currentDay</div>
                            </div>
                        }
                        currentDay++;
                    }
                    blockCount ++; 
                }
            </div>   
        }

    </div>
</div>

@* <h1>If you wish to request an event for the club to consider please fill out the form below. You can also see scheduled events on this page. </h1>

<div class="container mt-4">
    <form asp-action="Create" method="post">
        
        <div class="mb-3">
            <label asp-for="EventHost" class="form-label"></label>
            <input asp-for="EventHost" class="form-control" placeholder="Enter host name.">
            <div class="form-text">We'll never share your host details with anyone else.</div>
            <span asp-validation-for="EventHost" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="EventName" class="form-label"></label>
            <input asp-for="EventName" class="form-control" placeholder="Enter your event name." />
            <div class="form-text">We'll never share your host details with anyone else.</div>
            <span asp-validation-for="EventName" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="GuestCount" class="form-label"></label>
            <input asp-for="GuestCount" class="form-control">
            <span asp-validation-for="GuestCount" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="VenueName" class="form-label"></label>
            <input asp-for="VenueName" class="form-control">
            <span asp-validation-for="VenueName" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="DateofEvent" class="form-label"></label>
            <input asp-for="DateofEvent" class="form-control">
            <span asp-validation-for="DateofEvent" class="text-danger small"></span>
        </div>

        <div class="mb-3 form-check">
            <input asp-for="IsKidFriendly" class="form-check-input" type="checkbox">
            <label asp-for="IsKidFriendly" class="form-check-label"></label>
        </div>

        <button type="submit" class="btn btn-primary">Submit Event</button>
    </form>

    <div class="mt-5">
        <h3 class="mb-3">Events Calendar</h3>
        <!-- Embeds the FullCalendar UI -->
        <partial name="Calander" />
    </div>
</div>

@section Scripts {
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.10/index.global.min.js'></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var calendarEl = document.getElementById('calendar');
            var calendar = new FullCalendar.Calendar(calendarEl, {
                initialView: 'dayGridMonth',
                headerToolbar: {
                    left: 'prev,next today',
                    center: 'title',
                    right: 'dayGridMonth,timeGridWeek,timeGridDay'
                },
                events: '/Events/GetEvents' // Fetches events from our new controller endpoint
            });
            calendar.render();
        });
    </script>
} *@

<!--
<form>


  <div class="mb-3">
    <label for="exampleInputEmail1" class="form-label">Email address</label>
    <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
    <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
  </div>
  <div class="mb-3">
    <label for="exampleInputPassword1" class="form-label">Password</label>
    <input type="password" class="form-control" id="exampleInputPassword1">
  </div>
  <div class="mb-3 form-check">
    <input type="checkbox" class="form-check-input" id="exampleCheck1">
    <label class="form-check-label" for="exampleCheck1">Check me out</label>
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
</form>
--> 
